OPENAI_API_KEY=
OPENAI_MODEL=gpt-5.6
PORT=3000
# Comma-separated origins allowed to call the API from a browser.
# For local testing, the server already allows localhost:5500 and localhost:8000.
# For deployment, set this to your GitHub Pages origin, for example:
# ALLOWED_ORIGINS=https://writban.github.io
ALLOWED_ORIGINS=
